{
  "name": "202608-pyconkr-asgi-pyodide",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node scripts/sync-demo-assets.mjs && slidev build",
    "dev": "node scripts/sync-demo-assets.mjs && slidev --open",
    "export": "node scripts/sync-demo-assets.mjs && slidev export",
    "dev:live": "concurrently -k -n slides,uvicorn,streamlit,static -c cyan,green,magenta,yellow 'node scripts/sync-demo-assets.mjs && slidev --open' 'cd samples/runtime-agnostic-asgi-app && uv run --frozen uvicorn main:app --port 8000' 'cd samples/streamlit-demo && uv run --frozen streamlit run app.py --server.port 8501 --server.headless true' 'cd samples/runtime-agnostic-asgi-app && python3 -m http.server 8080'"
  },
  "dependencies": {
    "@iconify-json/ri": "^1.2.10",
    "@slidev/cli": "^52.19.0",
    "slidev-addon-fancy-arrow": "^0.16.1",
    "slidev-addon-qrcode": "^1.0.2",
    "slidev-addon-window-mockup": "^0.3.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
