[project]
name = "step3-cloudflare"
version = "0.1.0"
description = "Step 3: the shared FastAPI app on Cloudflare Python Workers"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "python-multipart>=0.0.32",
]

[dependency-groups]
dev = ["workers-py", "workers-runtime-sdk"]
